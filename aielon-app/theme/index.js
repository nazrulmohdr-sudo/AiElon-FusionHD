import { colors, spacing, fontSize, borderRadius, shadows } from './colors';

export {
  colors,
  spacing,
  fontSize,
  borderRadius,
  shadows,
};

export default {
  colors,
  spacing,
  fontSize,
  borderRadius,
  shadows,
};
